type User {
  id: ID!
  username: String!
  posts: [Post!]!
  comments: [Comment!]!
}

type AuthPayload {
  accessToken: String!
  user: User!
}

type Mutation {
  register(username: String!, password: String!): User!
  login(username: String!, password: String!): AuthPayload!
}
